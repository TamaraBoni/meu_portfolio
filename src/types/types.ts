export type TipoCheckpoints = {
  rm: number;
  nome: string;
  nota: number;
  materia: string;
  checkpoint: string;
};

export type TipoChallengersSprints = {
  rm: number;
  nome: string;
  nota: number;
  materia: string;
  sprint: number;
};
